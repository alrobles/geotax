% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_geotax_probability.R
\name{predict_geotax_probability}
\alias{predict_geotax_probability}
\title{Predict host susceptibility probabilities from logistic regression coefficients}
\usage{
predict_geotax_probability(coef, dist)
}
\arguments{
\item{coef}{A named numeric vector with elements \code{"intercept"} and
\code{"slope"}, OR a \code{geotax_model} object, OR a \code{geotax_bootstrap}
object (in which case the bootstrap summary means are used).
An unnamed vector of length >= 2 is interpreted as \code{c(intercept, slope)}.}

\item{dist}{A numeric vector of phylogenetic distances.}
}
\value{
A named numeric vector of predicted probabilities of the same length
  as \code{dist}.
}
\description{
Applies a logit function using intercept and slope coefficients to a vector
of phylogenetic distances. Accepts a numeric vector, a \code{geotax_model},
or a \code{geotax_bootstrap} object. This is the v2 replacement for
\code{prob_logit()}.
}
\examples{
inc <- prepare_incidence_matrix(beetleTreeInteractions)
al  <- align_geotax_inputs(inc, phy_dist)
model <- fit_geotax_model(al$incidence, al$phydist, seed = 42)
dist_vec <- seq(0, max(al$phydist), length.out = 50)
predict_geotax_probability(model, dist_vec)
}
\seealso{
\code{\link{prob_logit}} (deprecated), \code{\link{fit_geotax_model}}
}
